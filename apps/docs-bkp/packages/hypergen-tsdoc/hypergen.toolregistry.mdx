---
title: "ToolRegistry class"
icon: "cube"
description: "Centralized registry for managing tool instances and factories\n\nThe ToolRegistry provides a singleton pattern for managing tool lifecycle, including registration, discovery, caching, and cleanup."
---
{/* Do not edit this file. It is automatically generated by API Documenter. */}

[Home](./index.mdx) &gt; [hypergen](./hypergen.mdx) &gt; [ToolRegistry](./hypergen.toolregistry.mdx)

## ToolRegistry class

Centralized registry for managing tool instances and factories

The ToolRegistry provides a singleton pattern for managing tool lifecycle, including registration, discovery, caching, and cleanup.

**Signature:**

```typescript
export declare class ToolRegistry 
```

## Methods

## Methods

<ResponseField name="cleanupExpiredInstances()" required>
  Cleanup expired cached instances
</ResponseField>
<ResponseField name="clearCache()" required>
  Clear the instance cache
</ResponseField>
<ResponseField name="getByType(toolType)" required>
  Get all registered tools of a specific type
</ResponseField>
<ResponseField name="getCategories()" required>
  Get all categories
</ResponseField>
<ResponseField name="getInstance(options)" required>
  Get the singleton registry instance
</ResponseField>
<ResponseField name="getRegisteredTypes()" required>
  Get all tool types with registrations
</ResponseField>
<ResponseField name="getRegistration(toolType, name)" required>
  Get tool registration information
</ResponseField>
<ResponseField name="getStats()" required>
  Get registry statistics
</ResponseField>
<ResponseField name="isRegistered(toolType, name)" required>
  Check if a tool is registered
</ResponseField>
<ResponseField name="register(toolType, name, factory, metadata)" required>
  Register a tool factory
</ResponseField>
<ResponseField name="release(toolType, name, instance)" required>
  Release a tool instance (mark as not in use)
</ResponseField>
<ResponseField name="reset()" required>
  Reset the singleton instance (primarily for testing)
</ResponseField>
<ResponseField name="resolve(toolType, name, options)" required>
  Resolve a tool instance by type and name
</ResponseField>
<ResponseField name="search(criteria)" required>
  Search for tools based on criteria
</ResponseField>
<ResponseField name="unregister(toolType, name)" required>
  Unregister a tool
</ResponseField>

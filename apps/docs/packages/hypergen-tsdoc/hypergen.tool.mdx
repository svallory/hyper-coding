---
title: "Tool class"
icon: "cube"
description: "Abstract base class for all Recipe Step System tools\n\nThis class provides common functionality for Template, Action, CodeMod, and Recipe tools. Each tool type extends this class to implement specific execution logic."
---
{/* Do not edit this file. It is automatically generated by API Documenter. */}

[Home](./index.mdx) &gt; [hypergen](./hypergen.mdx) &gt; [Tool](./hypergen.tool.mdx)

## Tool class

Abstract base class for all Recipe Step System tools

This class provides common functionality for Template, Action, CodeMod, and Recipe tools. Each tool type extends this class to implement specific execution logic.

**Signature:**

```typescript
export declare abstract class Tool<TStep extends RecipeStepUnion = RecipeStepUnion> 
```

## Constructors

## Methods

<ResponseField name="(constructor)(toolType, name, options)" required>
  Constructs a new instance of the  class
</ResponseField>

## Properties

## Properties

<ParamField path="_isCleanedUp" type="boolean" required>
</ParamField>
<ParamField path="_isExecuting" type="boolean" required>
</ParamField>
<ParamField path="_isInitialized" type="boolean" required>
</ParamField>
<ParamField path="debug" type="ReturnType<typeof createDebug>" required>
</ParamField>
<ParamField path="logger" type="Logger" required>
</ParamField>
<ParamField path="metrics" type="ToolLifecycleMetrics" required>
</ParamField>
<ParamField path="name" type="string" required>
</ParamField>
<ParamField path="options" type="Record<string, any>" required>
</ParamField>
<ParamField path="resources" type="Map<string, ToolResource>" required>
</ParamField>
<ParamField path="toolType" type="ToolType" required>
</ParamField>

## Methods

## Methods

<ResponseField name="cleanup()" required>
  Clean up resources used by the tool
</ResponseField>
<ResponseField name="execute(step, context, options)" required>
  Execute the tool with the given step configuration and context
</ResponseField>
<ResponseField name="getMemoryUsage()" required>
  Get memory usage statistics
</ResponseField>
<ResponseField name="getMetrics()" required>
  Get tool lifecycle metrics
</ResponseField>
<ResponseField name="getName()" required>
  Get tool name
</ResponseField>
<ResponseField name="getToolType()" required>
  Get tool type
</ResponseField>
<ResponseField name="initialize()" required>
  Initialize the tool with configuration Called once before any execution
</ResponseField>
<ResponseField name="isCleanedUp()" required>
  Check if tool has been cleaned up
</ResponseField>
<ResponseField name="isExecuting()" required>
  Check if tool is currently executing
</ResponseField>
<ResponseField name="isInitialized()" required>
  Check if tool is initialized
</ResponseField>
<ResponseField name="onCleanup()" required>
  Tool-specific cleanup logic Override this method to perform any cleanup required after execution
</ResponseField>
<ResponseField name="onExecute(step, context, options)" required>
  Tool-specific execution logic Must be implemented by concrete tool classes
</ResponseField>
<ResponseField name="onInitialize()" required>
  Tool-specific initialization logic Override this method to perform any setup required before execution
</ResponseField>
<ResponseField name="onValidate(step, context)" required>
  Tool-specific validation logic Must be implemented by concrete tool classes
</ResponseField>
<ResponseField name="registerResource(resource)" required>
  Register a resource for cleanup
</ResponseField>
<ResponseField name="unregisterResource(resourceId)" required>
  Unregister a resource (useful for early cleanup)
</ResponseField>
<ResponseField name="validate(step, context)" required>
  Validate the step configuration and context
</ResponseField>

---
title: "Webpack"
description: "Integrate HyperDev with Webpack for template validation, auto-generation, and quality gates during the build process."
---

# Webpack Integration

The HyperDev Webpack plugin hooks into your build pipeline to handle template validation, auto-generation, and quality gates --- so you're not running them separately as pre-build scripts.

## Webpack Plugin Configuration

Add the plugin to your Webpack config:

```javascript
// webpack.config.js
const HyperDevPlugin = require('@hypercli/webpack-plugin')
const path = require('path')

module.exports = {
  // ... other config
  plugins: [
    new HyperDevPlugin({
      // Template validation during build
      validateTemplates: true,
      // Auto-generate files based on templates
      autoGenerate: {
        enabled: true,
        patterns: ['src/**/*.template.yml'],
        outputDir: 'src/generated'
      },
      // Quality gates integration
      qualityGates: {
        enabled: process.env.NODE_ENV === 'production',
        failOnError: true,
        rules: ['typescript', 'eslint', 'security', 'performance']
      },
      // Template hot reload in development
      hotReload: process.env.NODE_ENV === 'development'
    })
  ],
  module: {
    rules: [
      {
        test: /\.jig$/,
        use: '@hypercli/template-loader',
        exclude: /node_modules/
      }
    ]
  }
}
```

## Template Loader Configuration

Configure the HyperDev template loader to validate and cache templates as Webpack processes them:

```javascript
// webpack.config.js
module.exports = {
  module: {
    rules: [
      {
        test: /\.jig$/,
        use: {
          loader: '@hypercli/template-loader',
          options: {
            // Enable caching for better performance
            cache: true,
            // Validate templates during load
            validate: true,
            // Include source maps in development
            sourceMap: process.env.NODE_ENV === 'development'
          }
        }
      },
      {
        test: /\.template\.yml$/,
        use: {
          loader: '@hypercli/config-loader',
          options: {
            // Parse and validate template configuration
            validate: true,
            // Include in dependency graph
            track: true
          }
        }
      }
    ]
  }
}
```

## Development Configuration

Dev-specific Webpack config with hot reloading for templates:

```javascript
// webpack.dev.js
const { merge } = require('webpack-merge')
const common = require('./webpack.common.js')
const HyperDevPlugin = require('@hypercli/webpack-plugin')

module.exports = merge(common, {
  mode: 'development',
  devServer: {
    hot: true,
    // Proxy HyperDev dashboard
    proxy: {
      '/hyperdev': {
        target: 'http://localhost:4000',
        changeOrigin: true
      }
    }
  },
  plugins: [
    new HyperDevPlugin({
      dev: {
        // Enable hot reloading for templates
        hotReload: true,
        // Watch for template changes
        watchTemplates: true,
        // Live validation
        liveValidation: true
      }
    })
  ]
})
```

## Production Configuration

Production builds run strict validation and fail fast on any quality gate failure:

```javascript
// webpack.prod.js
const { merge } = require('webpack-merge')
const common = require('./webpack.common.js')
const HyperDevPlugin = require('@hypercli/webpack-plugin')

module.exports = merge(common, {
  mode: 'production',
  plugins: [
    new HyperDevPlugin({
      production: {
        // Strict validation in production
        strictValidation: true,
        // Optimize generated code
        optimizeOutput: true,
        // Fail build on quality gate failures
        failOnQualityGate: true,
        // Generate build report
        generateReport: true
      },
      qualityGates: {
        enabled: true,
        strict: true,
        rules: ['typescript', 'eslint', 'security', 'performance', 'accessibility']
      }
    })
  ]
})
```

## Custom Loaders Integration

Need to handle HyperDev schema files in a custom way? Here's a starting point for a custom loader:

```javascript
// loaders/hyperdev-schema-loader.js
module.exports = function(source) {
  const callback = this.async()
  const hyper = require('@hypercli/core')
  
  // Process HyperDev schema files
  hyper.processSchema(source)
    .then(result => {
      callback(null, result)
    })
    .catch(error => {
      callback(error)
    })
}
```

## Plugin Configuration Options

The full set of options for advanced configuration:

```javascript
// webpack.config.js
new HyperDevPlugin({
  // Template processing
  templates: {
    // Directory patterns to watch
    patterns: ['src/**/*.template.yml', 'templates/**/*.jig'],
    // Output directory for generated files
    outputDir: 'src/generated',
    // Template compilation options
    compile: {
      sourceMap: true,
      minify: process.env.NODE_ENV === 'production'
    }
  },
  
  // Validation settings
  validation: {
    // Validate on every build
    onBuild: true,
    // Validate on file changes (dev mode)
    onWatch: true,
    // Validation rules
    rules: {
      syntax: true,
      security: true,
      performance: true
    }
  },
  
  // Build integration
  build: {
    // Generate manifest file
    manifest: true,
    // Include metadata in output
    metadata: true,
    // Clean generated files before build
    clean: true
  }
})
```

## Build Scripts Integration

Package.json scripts to tie Webpack and HyperDev together:

```json
{
  "scripts": {
    "dev": "concurrently \"hyper watch\" \"webpack serve --config webpack.dev.js\"",
    "build": "webpack --config webpack.prod.js",
    "build:analyze": "webpack-bundle-analyzer dist/stats.json",
    "hyperdev:gen": "hyper gen"
  }
}
```

With this setup, your templates are validated and processed as part of the normal Webpack build --- no separate pipeline needed.

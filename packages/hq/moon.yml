type: library
language: typescript
platform: node
stack: backend

tags:
  - typescript
  - hyperdev
  - session-management

tasks:
  test:
    command: 'bun test'
    inputs:
      - '@globs(sources)'

toolchain:
  typescript:
    syncProjectReferences: false

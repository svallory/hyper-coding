# {{ projectName }}

{{ projectType | capitalize }} project generated with Hypergen.

## Getting Started

```bash
# Install dependencies
bun install

# Start development
bun run dev

{%- if includeTests %}
# Run tests
bun test

# Run tests with coverage
bun test:coverage
{%- else %}
# Type check
bun run typecheck
{%- endif %}

# Build for production
bun run build
```

## Project Structure

```
.
├── src/
│   └── index.ts
{%- if includeTests %}
├── test/
│   └── setup.ts
{%- endif %}
├── package.json
└── README.md
```
$schema: 'https://moonrepo.dev/schemas/project.json'
id: hyper
type: tool
language: typescript
platform: system
stack: infrastructure

tags:
  - workspace-root

tasks:
  install-command:
    script: bash $MOON_WORKSPACE_ROOT/scripts/install-command.sh
    options:
      shell: true
      runInCI: false
